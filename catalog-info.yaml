apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: test-api-explorer
  description: API de test pour valider le plugin API Explorer
spec:
  type: openapi
  lifecycle: experimental
  owner: group:default/frontend
  definition: |
    openapi: 3.0.0
    info:
      title: Test API
      version: 1.0.0
    paths:
      /health:
        get:
          responses:
            '200':
              description: OK
